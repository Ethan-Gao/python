1.4 数学与表达式
    a. 整数除法: 默认只取整数部分，因此1/2结果为0
    b. 小数/浮点数除法方式有两种
        b1. 分子或者分母有一个为小数/浮点数，例如1/2.0
        b2. 使用from __future__ import division，则/默认浮点，//整除
    c. 求余: %
        都为整数则结果整数，有一个小数则结果小数
    d. 幂: **
        幂运算优先级比取反优先级高，所以-3 ** 2 = - (3 ** 2)
    e. 长整型数据处理: python可自动处理非常大的数据，包括各种加减乘除运算
    f. 进制: 16进制 - 0x, 8进制 - 0
        各种进制可随意做各种运算，最后都转化为10进制

1.5 变量
    a. 可包含字母、数字、下划线，但是不能以数字开头
    b. 定义时，与C语言不同，需要写成a, b, ... =

1.7 获取用户输入
    有两个函数，input和raw_input，区别如下
    input: 仅能接受数值型输入，比如-1, 2.3, 12，返回为数字型
    raw_input: 能接受各种输入，比如数值，字符，混合等等，返回为字符型

1.8 函数
    a. 特别注意print，可当函数用，但是可以不用括号
        因此print "hello"与print("hello")作用相同
    b. abs-取绝对值，round-四舍五入

1.9 模块
    a. 导入函数，import和from ... import ...，区别如下
        import: 导入模块，按照"模块.函数"使用
        from ... import ...: 导入模块内的函数，按照"函数"使用
        因此同时from math import *和from cmath import *会冲突，有同名函数
    b. sqrt函数
        math.sqrt仅能计算实数浮点，cmath.sqrt能同时计算实数和虚数开方
    c. 虚数支持
        python默认支持，无须使用import cmath导入
    d. 函数引用
        当使用import math之后，可以直接使用变量来引用函数，比如x = math.sqrt
    e. 几个函数比较
        round-四舍五入，floor-下舍，ceil-上入

1.10 运行程序
    pycharm运行结束后会显示状态，但是如果直接点击，执行完毕后会推出，因此需要暂停
    加入以下内容即可，raw_input("press <enter>")

1.11 字符串
    a. 强制转换: 同C语言，直接使用str可转换任意类型到字符型
    b. 转义字符: 同C语言，直接使用\
    c. 拼接字符串: 直接使用+连接两个字符串即可
    d. str和repr区别
        d1. str打印人易读格式，repr打印python格式(即跟命令行结果一样)
        d2. ""可强制格式转换，作用同str，例如str(123)类型为字符型
    e. 长字符串
        使用三个单引号或者双引号, 使用\进行转义
    f. 原始字符串: r
        f1. 解决\转义字符引起的问题，例如"c:\nowhere"，由于\n构成换行
        因此需要使用"c:\\nowhere"或者r"c:\nowhere"
        f2. 特别注意，使用r时最后一个字符不能为\，否则需要拆分
            print r"c:\nowhere", r"c:\nowhere\"会报错
            print r"c:\nowhere", r"c:\nowhere\\"会打印\\
            print r"c:\nowhere", r"c:\nowhere" + "\\"
    g. unicode字符串: u
        普通字符串以8位ascii存储，unicode以16为存储，主要用于语言
        例如u"친구야안녕",u"こんにちは"

1.12 小结
    本章所有函数功能如下
    # 数据处理
    取绝对值 - abs(number) - 输入正负数字型，返回数字型绝对值
    四舍五入 - round(num[.ndigits]) - 根据精度ndigits对num进行四舍五入操作
    上入下舍 - math.floor(number) - 下舍，返回浮点型
              math.ceil(number) - 上入，返回浮点
    平方根 - math.sqrt(number) - 输入正数字型开方(只能正数，负数报错)，返回平方根
            cmath.sqrt(number) - 输入正负数字型(负数结果为复数)，返回平方根
    幂 - pow(x, y[. z]) - 返回x的y次幂(所得结果对z取模)
    # 类型转换
    同C语言，直接进行强制转换，如下
    float - 强制转换为浮点     int - 强制转换为整型
    long - 强制转换为长整型     str - 强制转换为字符型
    # 用户输入
    input - 接收数字型输入，返回数字型
    raw_input - 接收数字型、字符型输入，返回字符串
    # 打印结果
    str - 人类模式，适合人们查看的格式
    repr - python格式，与python命令行结果相同
    # 字符串处理
    转义 - \      原始字符串 - r       unicode字符串 - u
